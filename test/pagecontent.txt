<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio Slideshow</title>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    
    <!-- FontAwesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <style>
        body {
            background: #111;
            color: white;
            font-family: Arial, sans-serif;
        }
        .slideshow-container {
            max-width: 400px;
            height: 600px;
            margin: 50px auto;
            position: relative;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0px 0px 10px rgba(255, 255, 255, 0.2);
        }
        .slide {
            display: none;
            position: absolute;
            width: 100%;
            height: 100%;
            background: #222;
            text-align: center;
            padding: 50px;
            animation: fadeIn 0.5s ease-in-out;
        }
        .active {
            display: block;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        .progress-bar {
            height: 5px;
            background: white;
            position: absolute;
            top: 0;
            left: 0;
            width: 0;
        }
        .slide-content {
            position: absolute;
            bottom: 30px;
            width: 100%;
        }
        .nav-buttons {
            position: absolute;
            bottom: 15px;
            width: 100%;
            display: flex;
            justify-content: center;
            gap: 15px;
        }
        .nav-buttons i {
            cursor: pointer;
            font-size: 24px;
            color: white;
            transition: 0.3s;
        }
        .nav-buttons i:hover {
            color: #0d6efd;
        }
    </style>
</head>
<body>

    <div class="slideshow-container">
        <!-- Slide 1 -->
        <div class="slide active">
            <div class="progress-bar"></div>
            <h2><i class="fas fa-user"></i> About Me</h2>
            <p>I'm a passionate web developer with experience in creating beautiful and functional websites.</p>
            <div class="slide-content">
                <div class="nav-buttons">
                    <i class="fas fa-arrow-left prev"></i>
                    <i class="fas fa-arrow-right next"></i>
                </div>
            </div>
        </div>

        <!-- Slide 2 -->
        <div class="slide">
            <div class="progress-bar"></div>
            <h2><i class="fas fa-laptop-code"></i> My Skills</h2>
            <p>Expert in HTML, CSS, JavaScript, Bootstrap, and modern frameworks like React and Vue.</p>
            <div class="slide-content">
                <div class="nav-buttons">
                    <i class="fas fa-arrow-left prev"></i>
                    <i class="fas fa-arrow-right next"></i>
                </div>
            </div>
        </div>

        <!-- Slide 3 -->
        <div class="slide">
            <div class="progress-bar"></div>
            <h2><i class="fas fa-briefcase"></i> My Work</h2>
            <p>I've worked on various projects, including e-commerce, dashboards, and landing pages.</p>
            <div class="slide-content">
                <div class="nav-buttons">
                    <i class="fas fa-arrow-left prev"></i>
                    <i class="fas fa-arrow-right next"></i>
                </div>
            </div>
        </div>

        <!-- Slide 4 -->
        <div class="slide">
            <div class="progress-bar"></div>
            <h2><i class="fas fa-envelope"></i> Contact</h2>
            <p>Email: me@example.com<br>Phone: +123 456 7890</p>
            <div class="slide-content">
                <div class="nav-buttons">
                    <i class="fas fa-arrow-left prev"></i>
                    <i class="fas fa-arrow-right next"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        let slides = document.querySelectorAll('.slide');
        let currentSlide = 0;
        let progressInterval;
        
        function showSlide(index) {
            slides.forEach((slide, i) => {
                slide.classList.remove('active');
                if (i === index) {
                    slide.classList.add('active');
                    animateProgress(slide);
                }
            });
        }

        function animateProgress(slide) {
            let progressBar = slide.querySelector('.progress-bar');
            progressBar.style.width = "0%";
            clearInterval(progressInterval);
            let progress = 0;
            progressInterval = setInterval(() => {
                if (progress >= 100) {
                    clearInterval(progressInterval);
                    nextSlide();
                }
                progressBar.style.width = progress + "%";
                progress += 2;
            }, 100);
        }

        function nextSlide() {
            currentSlide = (currentSlide + 1) % slides.length;
            showSlide(currentSlide);
        }

        function prevSlide() {
            currentSlide = (currentSlide - 1 + slides.length) % slides.length;
            showSlide(currentSlide);
        }

        document.querySelectorAll('.next').forEach(btn => {
            btn.addEventListener('click', nextSlide);
        });

        document.querySelectorAll('.prev').forEach(btn => {
            btn.addEventListener('click', prevSlide);
        });

        // Auto-slide every 5 seconds
        setInterval(nextSlide, 5000);
    </script>

</body>
</html>
