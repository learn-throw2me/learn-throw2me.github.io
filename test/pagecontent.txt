<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Head Script Test</title>

    <!-- External Bootstrap JS (must load first) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Custom Script 1 (should execute after Bootstrap loads) -->
    <script>
        alert("Custom Script 1: Bootstrap should be available now.");
        if (typeof bootstrap !== "undefined") {
            alert("Bootstrap is loaded correctly.");
        } else {
            alert("Bootstrap did not load in time.");
        }
    </script>

    <!-- External Test Script -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.30.1/moment.min.js"></script>

    <!-- Custom Script 2 (should execute after Moment.js loads) -->
    <script>
        alert("Custom Script 2: Moment.js should be available now.");
        if (typeof moment !== "undefined") {
            alert("Moment.js is loaded correctly: " + moment().format());
        } else {
            alert("Moment.js did not load in time.");
        }
    </script>
</head>
<body>
    <h1>Head Script Order Test</h1>
    <button class="btn btn-primary" onclick="alert('Bootstrap is working!')">Click Me</button>
</body>
</html>
