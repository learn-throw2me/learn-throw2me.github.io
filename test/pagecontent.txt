<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complex Web Page</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.14/codemirror.min.css">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: Arial, sans-serif; background: #1e1e1e; color: white; }
        .container { max-width: 900px; margin: 20px auto; padding: 20px; background: #292929; border-radius: 5px; }
        .tabs { display: flex; border-bottom: 2px solid #555; }
        .tab { padding: 10px 20px; cursor: pointer; flex: 1; text-align: center; background: #333; }
        .tab.active { background: #444; border-bottom: 2px solid white; }
        .tab-content { display: none; padding: 20px; }
        .tab-content.active { display: block; }
        .task-list { list-style: none; padding: 10px; }
        .task { display: flex; justify-content: space-between; padding: 5px; background: #555; margin-bottom: 5px; }
        .task button { background: red; color: white; border: none; padding: 5px; cursor: pointer; }
        .editor-container { margin-top: 20px; }
        .CodeMirror { height: 200px; background: black; }
    </style>
</head>
<body>
    <div class="container">
        <div class="tabs">
            <div class="tab active" onclick="openTab(event, 'dashboard')">Dashboard</div>
            <div class="tab" onclick="openTab(event, 'editor')">Editor</div>
            <div class="tab" onclick="openTab(event, 'tasks')">Tasks</div>
        </div>
        <div id="dashboard" class="tab-content active">
            <h2>Dashboard</h2>
            <p>Welcome to the interactive dashboard.</p>
        </div>
        <div id="editor" class="tab-content">
            <h2>Code Editor</h2>
            <textarea id="code-editor"></textarea>
        </div>
        <div id="tasks" class="tab-content">
            <h2>Task Manager</h2>
            <input type="text" id="taskInput" placeholder="New Task">
            <button onclick="addTask()">Add</button>
            <ul class="task-list" id="taskList"></ul>
        </div>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.14/codemirror.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.14/mode/javascript/javascript.min.js"></script>
    <script>
        function openTab(evt, tabName) {
            document.querySelectorAll('.tab-content').forEach(el => el.classList.remove('active'));
            document.getElementById(tabName).classList.add('active');
            document.querySelectorAll('.tab').forEach(el => el.classList.remove('active'));
            evt.currentTarget.classList.add('active');
        }
        function addTask() {
            let input = document.getElementById('taskInput');
            if (input.value.trim() === '') return;
            let taskList = document.getElementById('taskList');
            let li = document.createElement('li');
            li.className = 'task';
            li.innerHTML = `<span>${input.value}</span> <button onclick="this.parentElement.remove()">Remove</button>`;
            taskList.appendChild(li);
            input.value = '';
        }
        document.addEventListener("DOMContentLoaded", function () {
            window.editor = CodeMirror.fromTextArea(document.getElementById("code-editor"), {
                mode: "javascript",
                theme: "dracula",
                lineNumbers: true
            });
        });
    </script>
</body>
</html>
